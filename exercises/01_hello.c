/*
 * CSC 139 - A0 C Warm-Up - Exercise 01: hello
 *
 * CONCEPT: A C program is text that must be compiled into a separate program before it runs.
 *
 * There is no `python hello.py` here. You write C, `gcc` turns it into a
 * machine-code file, and you run THAT file. `make run-01` does both steps.
 *
 * Three things every C program has, all of them already below:
 *   #include <stdio.h>   pulls in the declaration of printf
 *   int main(void)       where execution starts
 *   return 0;            0 means "finished successfully"
 *
 * printf does NOT add a newline for you. You have to write \n yourself.
 *
 * TODO: print the line below.
 *
 * Your program must print EXACTLY this:
 *
 *     hello, CSC 139
 *
 * Check yourself with:  make test-01        (or: make run-01 to just see it)
 */

#include <stdio.h>

int main(void)
{
    /* TODO: print  hello, CSC 139  followed by a newline. */

    return 0;
}
